networks:
  temporal-network:
    driver: bridge
    name: temporal-network
    external: true

services:

  producer:
    image: temporal-entity-demo/producer:latest
    deploy:
      replicas: 1
    environment:
      PULSAR_HOST: pulsar
    networks:
      - temporal-network